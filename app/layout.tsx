import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "RecoverAI Engine — Live Recovery Audit Dashboard",
  description:
    "AI-advised, policy-governed payment recovery for Indian eCommerce and SaaS: an advisory reasoning layer proposes an intervention, a deterministic RBI/TRAI-compliant policy engine decides, and every decision is written to an append-only audit ledger.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
